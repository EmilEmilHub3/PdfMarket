<UserControl x:Class="PdfMarket.AdminClient.Views.StatsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:PdfMarket.AdminClient.Infrastructure"
             mc:Ignorable="d">

    <UserControl.Resources>
        <!-- Bool -> Visibility -->
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top bar -->
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            
            <TextBlock Margin="12,0,0,0"
                       VerticalAlignment="Center"
                       Text="{Binding Progress, StringFormat=Progress: {0}%}" />
        </StackPanel>

        <!-- PROGRESS BAR  -->
        <ProgressBar Grid.Row="1"
                     Margin="0,10,0,0"
                     Height="16"
                     Minimum="0"
                     Maximum="100"
                     Value="{Binding Progress}"
                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}" />

        <!-- Loading tekst -->
        <TextBlock Grid.Row="2"
                   Margin="0,10,0,0"
                   FontSize="16"
                   FontWeight="Bold"
                   Text="Loading statistics..."
                   HorizontalAlignment="Center"
                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}" />

        <!-- Error tekst -->
        <TextBlock Grid.Row="3"
                   Margin="0,12,0,0"
                   Foreground="Red"
                   Text="{Binding ErrorMessage}" />

        <!-- Selve stats -->
        <StackPanel Grid.Row="3" Margin="0,40,0,0">
            <TextBlock FontSize="14"
                       Text="{Binding Stats.TotalUsers, StringFormat=Total users: {0}}" />
            <TextBlock FontSize="14"
                       Text="{Binding Stats.TotalPdfs, StringFormat=Total PDFs: {0}}" />
            <TextBlock FontSize="14"
                       Text="{Binding Stats.TotalPurchases, StringFormat=Total purchases: {0}}" />
            <TextBlock FontSize="14"
                       Text="{Binding Stats.TotalPointsInSystem, StringFormat=Total points in system: {0}}" />
        </StackPanel>
    </Grid>
</UserControl>
