<Window x:Class="PdfMarket.AdminClient.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Admin Login" Height="250" Width="400">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Admin Login" FontSize="18" Margin="0,0,0,8"/>
            <TextBlock Text="Username or email"/>
            <TextBox Text="{Binding UserNameOrEmail, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text="Password" Margin="0,8,0,0"/>
            <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_OnPasswordChanged"/>
        </StackPanel>

        <Button Grid.Row="1" Content="Login" Height="30" Width="100" HorizontalAlignment="Left"
                Margin="0,8,0,0"
                Command="{Binding LoginCommand}"/>

    </Grid>
</Window>
