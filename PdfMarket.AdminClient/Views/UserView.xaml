<UserControl x:Class="PdfMarket.AdminClient.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.ColumnSpan="2">
            <Button Content="Refresh" Width="100" Margin="0,0,8,0"
                    Command="{Binding RefreshCommand}" />
            <Button Content="Save changes" Width="120"
                    Command="{Binding SaveCommand}" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="12,6,0,0"/>
        </StackPanel>

        <DataGrid Grid.Row="1" Grid.Column="0"
                  ItemsSource="{Binding Users}"
                  SelectedItem="{Binding SelectedUser}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Margin="0,8,8,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="UserName" Binding="{Binding UserName}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Points" Binding="{Binding PointsBalance}" Width="Auto"/>
                <DataGridTextColumn Header="Uploads" Binding="{Binding UploadCount}" Width="Auto"/>
                <DataGridTextColumn Header="Buys" Binding="{Binding PurchaseCount}" Width="Auto"/>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="1" Grid.Column="1" Margin="0,8,0,0" Padding="12" BorderThickness="1" BorderBrush="LightGray">
            <StackPanel>
                <TextBlock Text="Edit selected user" FontWeight="Bold" Margin="0,0,0,8"/>

                <TextBlock Text="Email"/>
                <TextBox Text="{Binding EditEmail, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                <TextBlock Text="Points"/>
                <TextBox Text="{Binding EditPoints, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                <TextBlock Foreground="Gray" TextWrapping="Wrap"
                           Text="Tip: Save will call PATCH /api/admin/users/{userId}."/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
