<UserControl x:Class="PdfMarket.AdminClient.Views.PdfModerationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <Button Content="Refresh" Width="100" Margin="0,0,8,0"
                    Command="{Binding RefreshCommand}" />
            <Button Content="Delete selected" Width="140"
                    Command="{Binding DeleteSelectedCommand}" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="12,6,0,0"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Margin="0,8,0,8" Foreground="Gray"
                   Text="All PDFs (admin). Select one and delete if needed." />

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Pdfs}"
                  SelectedItem="{Binding SelectedPdf}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="2*"/>
                <DataGridTextColumn Header="Uploader" Binding="{Binding UploaderUserName}" Width="*"/>
                <DataGridTextColumn Header="Price" Binding="{Binding PriceInPoints}" Width="Auto"/>
                <DataGridTextColumn Header="Created" Binding="{Binding CreatedAt}" Width="Auto"/>
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="Auto"/>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
